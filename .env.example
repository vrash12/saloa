# Empty = email-draft mode when siteConfig.email is set; otherwise preview mode.
# Drafts are sent by the visitor from their email app, not by this website.
# Endpoint must accept POST JSON and return { "accepted": true } after storing
# the inquiry or durably enqueueing delivery. Never place secrets in VITE_ vars.
VITE_CONTACT_ENDPOINT=
